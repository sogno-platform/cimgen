#ifndef {{class_name}}_H
#define {{class_name}}_H
/*
Generated from the CGMES files via cimgen: https://github.com/sogno-platform/cimgen
*/

#include <istream>
#include <ostream>
#include <string>

namespace CIMPP
{
{{#class_comment}}
	/** \brief {{{class_comment}}} */
{{/class_comment}}
	class {{class_name}}
	{
	public:
		{{class_name}}() : initialized(false) {}
		{{class_name}}(const std::string& value) : value(value), initialized(true) {}

		{{class_name}}& operator=(const std::string &rop);
		operator std::string() const;

		std::string value;
		bool initialized;

		static const char debugName[];
		const char* debugString() const;

		friend std::istream& operator>>(std::istream& lop, {{class_name}}& rop);
		friend std::ostream& operator<<(std::ostream& os, const {{class_name}}& obj);
	};
}
#endif
